1. parse CLI:
  if no arguments:
    print usage and exit
  if flag -a:
    if ac > 2:
       output bytecode to stdout
    else:
       Can't read source file (null)
  if ac > 2:
    open the last filename in argv

2. Parse file:
  * permission denied
  * empty file
  * bad magic
  bot name
  2 NULL bytes
  bot size
  comment
  bot comment (isn't it the same as comment)
  2 NULL bytes after comment

3. Translate executable code:


4. Save to file with the same filename, extension *.cor